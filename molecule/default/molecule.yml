---
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  set -e
  yamllint .
  ansible-lint
platforms:
  - name: "generate-ssh-keys-${image:-ubuntu2204}-${tag:-latest}${TOX_ENVNAME}"
    image: geerlingguy/docker-${image:-ubuntu2204}-ansible:${tag:-latest}
    command: ""
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    privileged: true
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
